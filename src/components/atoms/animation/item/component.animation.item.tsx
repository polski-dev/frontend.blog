import React from "react";
import { Item } from "./component.animation.item.style";

export const ItemLoad = ({ height, last, style }: { height: number; last: boolean; style: any }) => <Item height={2.5} style={style} last={last} />;
