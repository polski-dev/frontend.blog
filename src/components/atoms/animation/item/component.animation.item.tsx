import React from "react";
import { Item } from "./component.animation.item.style";

export const ItemLoad = ({ height, last }: { height: number; last: boolean }) => <Item height={2.5} last={last} />;
