import React from "react";
import { Circle } from "./component.animation.circle.style";

export const ComponentAnimationCircleLoad = ({ size = 1 }: { size?: number }): JSX.Element => <Circle size={size} />;
