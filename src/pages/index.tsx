import type { NextPage } from "next";
import Layout from "layout/layout.index";

const Home: NextPage = () => {
  return (
    <Layout>
      <h2>Blog</h2>
    </Layout>
  );
};

export default Home;
